function calculaDistancia(x2, x1, y2, y1) {
  let pontox = x2 - x1;
  let pontoy = y2 - y1;
  let res = Math.sqrt(pontox** 2 + pontoy ** 2);
  return console.log(res);
}
calculaDistancia(1, 1, 4, 1);

